services:
  - type: web
    name: openai-assistant-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn isooko:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: ASSISTANT_ID
        sync: false